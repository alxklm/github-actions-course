name: 03 - GitHub Runners
on: workflow_dispatch

jobs:
    ubuntu-runner:
        runs-on: ubuntu-latest
        steps:
            - name: Show OS
              run: |
                  echo "Running on $RUNNER_OS"
                  echo "This job running on Ubuntu"
    windows-runner:
        runs-on: windows-latest
        steps:
            - name: Show OS
              shell: bash
              run: |
                  echo "This job running on Windows"
                  echo "Running on $RUNNER_OS"
